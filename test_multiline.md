# 複数行入力のテスト

REPLで以下のように入力できるようになりました：

```scheme
Scheme>>> (define (fib n)
      ...   (if (< n 2)
      ...       n
      ...       (+ (fib (- n 1)) (fib (- n 2)))))
(lambda (n) ...)
Scheme>>> (fib 10)
55
```

括弧が閉じていない場合、自動的に継続入力モード（`... `プロンプト）になります。
